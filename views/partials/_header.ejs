<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>

      </div>

    </div>
    <% if (typeof(user) =='undefined' ) { %>
    <form class="form-inline" action="/login" method="POST">
      <div class="form-group mb-2">
        <label for="username">Username:</label>
        <input class="form-control" type="text" name="username" placeholder="eg. URLslayer3000"
          style="width: 400px; margin: 1em" />
        <button type="submit" class="btn btn-primary">Submit</button>
        <% } %>
      </div>

      <div>
        <% if (typeof(user) !='undefined' ) { %>

      </form>
      <label for="logout">Signed in as <i><b><%= user.email%></b></i>:</label>
      <form method="POST" action="/logout">
      <td><button name="logout" type="submit" class="btn btn-primary"
          style="background-color:rgb(232, 56, 3)">Logout</button></td>
    </form>
    <% } %>
      </div>
  </nav>
</header>